import { useEffect, useState } from 'react'
import './App.css'

function App() {
  const [screen, setScreen] = useState('splash')
  const [progress, setProgress] = useState(0)

  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [loginError, setLoginError] = useState('')

  const [fullName, setFullName] = useState('')
  const [registerEmail, setRegisterEmail] = useState('')
  const [registerPassword, setRegisterPassword] = useState('')
  const [confirmPassword, setConfirmPassword] = useState('')
  const [agreeTerms, setAgreeTerms] = useState(false)
  const [registerError, setRegisterError] = useState('')

  const [showWarningDetails, setShowWarningDetails] = useState(false)
  const [historySearch, setHistorySearch] = useState('')
  const [historyFilter, setHistoryFilter] = useState('all')

  const scanHistory = [
    {
      id: 1,
      domain: 'stripe.com',
      url: 'https://pay.stripe.com/checkout...',
      status: 'safe',
      time: 'Jun 24, 2026 · 09:42 AM',
    },
    {
      id: 2,
      domain: 'amaz0n-deals.ru',
      url: 'http://amaz0n-deals.ru/promo',
      status: 'blocked',
      time: 'Jun 24, 2026 · 08:17 AM',
    },
    {
      id: 3,
      domain: 'github.com',
      url: 'https://github.com/vafpqr/app',
      status: 'safe',
      time: 'Jun 23, 2026 · 03:55 PM',
    },
    {
      id: 4,
      domain: 'whatsapp.com',
      url: 'https://wa.me/qr/ABCDEFG',
      status: 'safe',
      time: 'Jun 23, 2026 · 11:20 AM',
    },
  ]

  const filteredHistory = scanHistory.filter((item) => {
    const searchText = historySearch.toLowerCase()

    const matchesSearch =
      item.domain.toLowerCase().includes(searchText) ||
      item.url.toLowerCase().includes(searchText)

    const matchesFilter =
      historyFilter === 'all' || item.status === historyFilter

    return matchesSearch && matchesFilter
  })

  useEffect(() => {
    if (screen !== 'splash') return

    const timer = setInterval(() => {
      setProgress((prev) => {
        if (prev >= 100) {
          clearInterval(timer)

          setTimeout(() => {
            setScreen('onboard1')
          }, 500)

          return 100
        }

        return prev + 2
      })
    }, 40)

    return () => clearInterval(timer)
  }, [screen])

  function handleSignIn() {
    const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/

    if (!email.trim()) {
      setLoginError('Please enter your email address.')
      return
    }

    if (!emailPattern.test(email)) {
      setLoginError('Please enter a valid email address.')
      return
    }

    if (!password) {
      setLoginError('Please enter your password.')
      return
    }

    if (password.length < 6) {
      setLoginError('Password must be at least 6 characters.')
      return
    }

    setLoginError('')
    setScreen('dashboard')
  }

  function handleCreateAccount() {
    const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/

    if (!fullName.trim()) {
      setRegisterError('Please enter your full name.')
      return
    }

    if (!registerEmail.trim()) {
      setRegisterError('Please enter your email address.')
      return
    }

    if (!emailPattern.test(registerEmail)) {
      setRegisterError('Please enter a valid email address.')
      return
    }

    if (!registerPassword) {
      setRegisterError('Please enter your password.')
      return
    }

    if (registerPassword.length < 8) {
      setRegisterError('Password must be at least 8 characters.')
      return
    }

    if (!confirmPassword) {
      setRegisterError('Please confirm your password.')
      return
    }

    if (registerPassword !== confirmPassword) {
      setRegisterError('Passwords do not match.')
      return
    }

    if (!agreeTerms) {
      setRegisterError(
        'Please agree to the Terms of Service and Privacy Policy.'
      )
      return
    }

    setRegisterError('')
    setScreen('dashboard')
  }

  function handleDemoScan() {
    const result = Math.random() > 0.5 ? 'safe-result' : 'warning-result'
    setShowWarningDetails(false)
    setScreen(result)
  }

  function handleSignOut() {
    setEmail('')
    setPassword('')
    setLoginError('')
    setScreen('login')
  }

  function BottomNav({ active }) {
    return (
      <div className="bottom-nav">
        <button
          className={active === 'dashboard' ? 'nav-item active' : 'nav-item'}
          type="button"
          onClick={() => setScreen('dashboard')}
        >
          <span>⌂</span>
          <p>Home</p>
        </button>

        <button
          className={active === 'scan' ? 'nav-item active' : 'nav-item'}
          type="button"
          onClick={() => setScreen('scan')}
        >
          <span>⌗</span>
          <p>Scan</p>
        </button>

        <button
          className={active === 'history' ? 'nav-item active' : 'nav-item'}
          type="button"
          onClick={() => setScreen('history')}
        >
          <span>↺</span>
          <p>History</p>
        </button>

        <button
          className={active === 'profile' ? 'nav-item active' : 'nav-item'}
          type="button"
          onClick={() => setScreen('profile')}
        >
          <span>☻</span>
          <p>Profile</p>
        </button>
      </div>
    )
  }

  return (
    <main className="app">
      <section className="phone">
        <div className="notch"></div>

        {screen === 'splash' && (
          <>
            <div className="splash-content">
              <div className="logo-box"></div>

              <h1>VAFPQR</h1>
              <p className="subtitle">SECURE QR CODE SCANNING</p>

              <div className="loading-dots">
                <span></span>
                <span></span>
                <span></span>
              </div>
            </div>

            <div className="loading-area">
              <div className="loading-text">
                <span>Initializing</span>
                <span>{progress}%</span>
              </div>

              <div className="progress-bar">
                <div
                  className="progress-fill"
                  style={{ width: `${progress}%` }}
                ></div>
              </div>

              <p className="version">
                v2.4.1 · Protected by end-to-end encryption
              </p>
            </div>
          </>
        )}

        {screen === 'onboard1' && (
          <div className="onboard-screen">
            <button className="skip-button" onClick={() => setScreen('login')}>
              Skip
            </button>

            <div className="onboard-card blue-card">
              <div className="icon-placeholder qr-icon"></div>
              <div className="tag blue-tag">REAL-TIME PROTECTION</div>

              <h2>Scan QR Codes Safely</h2>

              <p>
                Every QR code you scan is instantly analyzed against our threat
                intelligence database before any action is taken.
              </p>
            </div>

            <div className="onboard-bottom">
              <div className="page-dots">
                <span className="active"></span>
                <span></span>
                <span></span>
              </div>

              <button
                className="next-button"
                onClick={() => setScreen('onboard2')}
              >
                Next ›
              </button>
            </div>
          </div>
        )}

        {screen === 'onboard2' && (
          <div className="onboard-screen">
            <button
              className="back-button"
              onClick={() => setScreen('onboard1')}
            >
              ‹
            </button>

            <button className="skip-button" onClick={() => setScreen('login')}>
              Skip
            </button>

            <div className="onboard-card amber-card">
              <div className="icon-placeholder warning-icon"></div>
              <div className="tag amber-tag">AI-POWERED DETECTION</div>

              <h2>Detect Malicious QR Codes</h2>

              <p>
                Advanced AI detects phishing URLs, malware distribution links,
                and credential harvesting attempts hidden in QR codes.
              </p>
            </div>

            <div className="onboard-bottom">
              <div className="page-dots">
                <span></span>
                <span className="active"></span>
                <span></span>
              </div>

              <button
                className="next-button"
                onClick={() => setScreen('onboard3')}
              >
                Next ›
              </button>
            </div>
          </div>
        )}

        {screen === 'onboard3' && (
          <div className="onboard-screen">
            <button
              className="back-button"
              onClick={() => setScreen('onboard2')}
            >
              ‹
            </button>

            <button className="skip-button" onClick={() => setScreen('login')}>
              Skip
            </button>

            <div className="onboard-card green-card">
              <div className="icon-placeholder lock-icon"></div>
              <div className="tag green-tag">ZERO-KNOWLEDGE PRIVACY</div>

              <h2>Protect Your Information</h2>

              <p>
                Your personal data and device are shielded. We never share scan
                data with third parties — privacy is our core principle.
              </p>
            </div>

            <div className="onboard-bottom">
              <div className="page-dots">
                <span></span>
                <span></span>
                <span className="active"></span>
              </div>

              <button
                className="next-button get-started-button"
                onClick={() => setScreen('login')}
              >
                Get Started ›
              </button>
            </div>
          </div>
        )}

        {screen === 'login' && (
          <div className="login-screen">
            <div className="login-brand">
              <div className="login-logo"></div>
              <span>VAFPQR</span>
            </div>

            <div className="login-header">
              <h2>Welcome back</h2>
              <p>Sign in to your secure account</p>
            </div>

            <div className="form-group">
              <label>Email Address</label>

              <div className="input-box">
                <input
                  type="email"
                  placeholder="alex@example.com"
                  value={email}
                  onChange={(event) => {
                    setEmail(event.target.value)
                    setLoginError('')
                  }}
                />
              </div>
            </div>

            <div className="form-group">
              <label>Password</label>

              <div className="input-box">
                <input
                  type="password"
                  placeholder="Enter your password"
                  value={password}
                  onChange={(event) => {
                    setPassword(event.target.value)
                    setLoginError('')
                  }}
                />
              </div>
            </div>

            <button className="forgot-button" type="button">
              Forgot password?
            </button>

            {loginError && <p className="form-error">{loginError}</p>}

            <button className="primary-button" onClick={handleSignIn}>
              Sign In Securely
            </button>

            <div className="divider">
              <span></span>
              <p>or continue with</p>
              <span></span>
            </div>

            <div className="social-row">
              <button type="button">Google</button>
              <button type="button">Apple</button>
            </div>

            <p className="bottom-link-text">
              Don&apos;t have an account?
              <button type="button" onClick={() => setScreen('register')}>
                Create Account
              </button>
            </p>
          </div>
        )}

        {screen === 'register' && (
          <div className="register-screen">
            <button
              className="register-back-button"
              type="button"
              onClick={() => setScreen('login')}
            >
              <span className="back-arrow">‹</span>
              <span>Back to Login</span>
            </button>

            <div className="register-header">
              <h2>Create Account</h2>
              <p>Join VAFPQR for protected QR scanning</p>
            </div>

            <div className="form-group">
              <label>Full Name</label>

              <div className="input-box register-input-box">
                <span className="input-icon">♙</span>

                <input
                  type="text"
                  placeholder="Alex Johnson"
                  value={fullName}
                  onChange={(event) => {
                    setFullName(event.target.value)
                    setRegisterError('')
                  }}
                />
              </div>
            </div>

            <div className="form-group">
              <label>Email Address</label>

              <div className="input-box register-input-box">
                <span className="input-icon">◎</span>

                <input
                  type="email"
                  placeholder="alex@example.com"
                  value={registerEmail}
                  onChange={(event) => {
                    setRegisterEmail(event.target.value)
                    setRegisterError('')
                  }}
                />
              </div>
            </div>

            <div className="form-group">
              <label>Password</label>

              <div className="input-box register-input-box">
                <span className="input-icon">▢</span>

                <input
                  type="password"
                  placeholder="Min. 8 characters"
                  value={registerPassword}
                  onChange={(event) => {
                    setRegisterPassword(event.target.value)
                    setRegisterError('')
                  }}
                />
              </div>
            </div>

            <div className="form-group">
              <label>Confirm Password</label>

              <div className="input-box register-input-box">
                <span className="input-icon">▢</span>

                <input
                  type="password"
                  placeholder="Repeat password"
                  value={confirmPassword}
                  onChange={(event) => {
                    setConfirmPassword(event.target.value)
                    setRegisterError('')
                  }}
                />
              </div>
            </div>

            <button
              className="terms-row"
              type="button"
              onClick={() => {
                setAgreeTerms(!agreeTerms)
                setRegisterError('')
              }}
            >
              <span className={agreeTerms ? 'checkbox checked' : 'checkbox'}>
                {agreeTerms ? '✓' : ''}
              </span>

              <span>
                I agree to the <u>Terms of Service</u> and{' '}
                <u>Privacy Policy</u>
              </span>
            </button>

            {registerError && <p className="form-error">{registerError}</p>}

            <button className="primary-button" onClick={handleCreateAccount}>
              Create Secure Account
            </button>

            <p className="bottom-link-text register-bottom-text">
              Already have an account?
              <button type="button" onClick={() => setScreen('login')}>
                Sign In
              </button>
            </p>
          </div>
        )}

        {screen === 'not-installed' && (
          <div className="not-installed-screen">
            <div className="install-content">
              <div className="install-logo">
                <span>♢</span>
              </div>

              <h2>VAFPQR Required</h2>

              <p className="install-subtitle">
                This QR code links to a protected destination.
                <br />
                Install VAFPQR to scan it safely.
              </p>

              <div className="warning-box">
                <span className="warning-mark">⚠</span>
                <p>
                  Opening unknown QR codes without security verification can
                  expose your device to phishing attacks and malware.
                </p>
              </div>

              <h3>Why use VAFPQR?</h3>

              <div className="feature-card">
                <div className="feature-icon blue-feature">♢</div>
                <div>
                  <h4>Real-time Threat Analysis</h4>
                  <p>AI-powered scan against 50M+ known threats</p>
                </div>
              </div>

              <div className="feature-card">
                <div className="feature-icon green-feature">▢</div>
                <div>
                  <h4>Zero-Data Policy</h4>
                  <p>We never store your scan history on our servers</p>
                </div>
              </div>

              <div className="feature-card">
                <div className="feature-icon yellow-feature">ϟ</div>
                <div>
                  <h4>Instant Results</h4>
                  <p>Sub-second verification with actionable insights</p>
                </div>
              </div>

              <div className="feature-card">
                <div className="feature-icon purple-feature">☆</div>
                <div>
                  <h4>4.9★ on App Stores</h4>
                  <p>Trusted by 2.3 million users worldwide</p>
                </div>
              </div>

              <button className="google-play-button" type="button">
                <span>▲</span>
                Get it on Google Play
              </button>

              <button className="app-store-button" type="button">
                <span>●</span>
                Download on the App Store
              </button>

              <button
                className="back-demo-button"
                type="button"
                onClick={() => setScreen('login')}
              >
                Back to Login
              </button>
            </div>
          </div>
        )}

        {screen === 'dashboard' && (
          <div className="dashboard-screen">
            <div className="dashboard-content">
              <div className="dashboard-header">
                <div>
                  <p className="welcome-label">Welcome Back</p>
                  <h2>Alex Johnson</h2>
                </div>

                <button
                  className="avatar-button"
                  type="button"
                  onClick={() => setScreen('profile')}
                >
                  A
                </button>
              </div>

              <div className="status-card">
                <div className="status-card-top">
                  <div className="status-title">
                    <span>♢</span>
                    <p>Security Status</p>
                  </div>

                  <span className="active-pill">ACTIVE</span>
                </div>

                <div className="status-stats">
                  <div>
                    <strong>142</strong>
                    <p>Total Scans</p>
                  </div>

                  <div>
                    <strong>138</strong>
                    <p>Safe</p>
                  </div>

                  <div>
                    <strong>4</strong>
                    <p>Blocked</p>
                  </div>
                </div>
              </div>

              <button
                className="scan-main-button"
                type="button"
                onClick={() => setScreen('scan')}
              >
                <span>⌗</span>
                Scan QR Code
              </button>

              <div className="recent-header">
                <h3>Recent Scans</h3>

                <button type="button" onClick={() => setScreen('history')}>
                  View all ›
                </button>
              </div>

              <div className="scan-list">
                <div className="scan-row">
                  <div className="scan-icon safe-icon">♢</div>

                  <div className="scan-info">
                    <h4>stripe.com</h4>
                    <p>https://pay.stripe.com/ch...</p>
                  </div>

                  <div className="scan-meta">
                    <span className="safe-badge">SAFE</span>
                    <p>2 min ago</p>
                  </div>
                </div>

                <div className="scan-row">
                  <div className="scan-icon blocked-icon">♢</div>

                  <div className="scan-info">
                    <h4>amaz0n-deals.ru</h4>
                    <p>http://amaz0n-deals.ru...</p>
                  </div>

                  <div className="scan-meta">
                    <span className="blocked-badge">BLOCKED</span>
                    <p>1 hr ago</p>
                  </div>
                </div>

                <div className="scan-row">
                  <div className="scan-icon safe-icon">♢</div>

                  <div className="scan-info">
                    <h4>github.com</h4>
                    <p>https://github.com/vafpqr...</p>
                  </div>

                  <div className="scan-meta">
                    <span className="safe-badge">SAFE</span>
                    <p>3 hrs ago</p>
                  </div>
                </div>
              </div>
            </div>

            <BottomNav active="dashboard" />
          </div>
        )}

        {screen === 'scan' && (
          <div className="scanner-screen">
            <div className="scanner-top-bar">
              <button
                className="scanner-circle-button"
                type="button"
                onClick={() => setScreen('dashboard')}
              >
                ×
              </button>

              <h2>QR Scanner</h2>

              <button className="scanner-circle-button" type="button">
                ϟ
              </button>
            </div>

            <div className="scanner-area">
              <div className="scanner-frame">
                <div className="scanner-corner scanner-corner-tl"></div>
                <div className="scanner-corner scanner-corner-tr"></div>
                <div className="scanner-corner scanner-corner-bl"></div>
                <div className="scanner-corner scanner-corner-br"></div>

                <div className="scanner-line"></div>
              </div>

              <div className="scanner-text">
                <h3>Align QR code within the frame</h3>
                <p>Scanning will start automatically</p>
              </div>
            </div>

            <div className="scanner-bottom-actions">
              <button
                className="scanner-small-action"
                type="button"
                onClick={handleDemoScan}
              >
                <span>▧</span>
                <p>Gallery</p>
              </button>

              <button
                className="scanner-camera-button"
                type="button"
                onClick={handleDemoScan}
              >
                ▣
              </button>

              <button className="scanner-small-action" type="button">
                <span>⋮</span>
                <p>More</p>
              </button>
            </div>
          </div>
        )}

        {screen === 'safe-result' && (
          <div className="safe-result-screen">
            <button
              className="result-back-button"
              type="button"
              onClick={() => setScreen('dashboard')}
            >
              ← Back
            </button>

            <div className="safe-result-content">
              <div className="safe-check-circle">
                <span>✓</span>
              </div>

              <div className="safe-pill">SAFE QR CODE</div>

              <h2>Verification Passed</h2>

              <p className="safe-description">
                This QR code has been verified safe. No threats, phishing
                attempts, or malware detected.
              </p>

              <div className="destination-card">
                <p className="destination-label">Destination URL</p>

                <h3>
                  https://pay.stripe.com/checkout/abc1
                  <br />
                  23xyz
                </h3>

                <div className="destination-divider"></div>

                <div className="security-row">
                  <span>♢ SSL Valid</span>
                  <span>◎ stripe.com</span>
                  <span>✓ No threats</span>
                </div>
              </div>

              <button className="open-link-button" type="button">
                ↗ Open Link
              </button>

              <div className="safe-action-row">
                <button type="button">▱ Save Scan</button>
                <button type="button">⌯ Share</button>
              </div>

              <button
                className="safe-dashboard-link"
                type="button"
                onClick={() => setScreen('dashboard')}
              >
                Back to Dashboard
              </button>
            </div>
          </div>
        )}

        {screen === 'warning-result' && (
          <div className="warning-result-screen">
            <button
              className="result-back-button warning-back-button"
              type="button"
              onClick={() => setScreen('dashboard')}
            >
              ← Back
            </button>

            <div className="warning-result-content">
              <div className="warning-alert-circle">
                <span>!</span>
              </div>

              <div className="warning-pill">THREAT DETECTED</div>

              <h2>Potentially Unsafe QR Code</h2>

              <p className="warning-description">
                This QR code points to a known phishing domain. Access has been
                blocked to protect your device.
              </p>

              <div className="threat-card">
                <div className="threat-row">
                  <span>Threat Type</span>
                  <strong>Phishing / Credential Harvesting</strong>
                </div>

                <div className="threat-row">
                  <span>Risk Level</span>
                  <strong>HIGH</strong>
                </div>

                <div className="threat-row">
                  <span>Domain</span>
                  <strong>amaz0n-deals.ru</strong>
                </div>

                <div className="threat-row">
                  <span>Reported by</span>
                  <strong>2,847 users</strong>
                </div>
              </div>

              {showWarningDetails && (
                <div className="warning-details-card">
                  <h3>Why is this unsafe?</h3>

                  <ul>
                    <li>Domain registered 3 days ago</li>
                    <li>Impersonating Amazon brand assets</li>
                    <li>SSL certificate from untrusted authority</li>
                    <li>Redirects to credential harvesting form</li>
                  </ul>
                </div>
              )}

              <button className="block-access-button" type="button">
                ♢ Block Access
              </button>

              <button
                className="warning-outline-button danger-outline"
                type="button"
                onClick={() => setShowWarningDetails(!showWarningDetails)}
              >
                ⓘ {showWarningDetails ? 'Hide Details' : 'View Details'}
              </button>

              <button
                className="warning-outline-button report-outline"
                type="button"
              >
                ⚐ Report QR Code
              </button>

              <button
                className="warning-dashboard-link"
                type="button"
                onClick={() => setScreen('dashboard')}
              >
                Back to Dashboard
              </button>
            </div>
          </div>
        )}

        {screen === 'history' && (
          <div className="history-screen">
            <div className="history-content">
              <div className="history-header">
                <h2>Scan History</h2>

                <button className="history-filter-icon" type="button">
                  ▽
                </button>
              </div>

              <div className="history-search-box">
                <span>⌕</span>
                <input
                  type="text"
                  placeholder="Search domains..."
                  value={historySearch}
                  onChange={(event) => setHistorySearch(event.target.value)}
                />
              </div>

              <div className="history-filter-row">
                <button
                  className={historyFilter === 'all' ? 'active' : ''}
                  type="button"
                  onClick={() => setHistoryFilter('all')}
                >
                  All
                </button>

                <button
                  className={historyFilter === 'safe' ? 'active' : ''}
                  type="button"
                  onClick={() => setHistoryFilter('safe')}
                >
                  Safe
                </button>

                <button
                  className={historyFilter === 'blocked' ? 'active' : ''}
                  type="button"
                  onClick={() => setHistoryFilter('blocked')}
                >
                  Blocked
                </button>
              </div>

              <div className="history-list">
                {filteredHistory.map((item) => (
                  <div className="history-card" key={item.id}>
                    <div
                      className={
                        item.status === 'safe'
                          ? 'history-status-icon safe'
                          : 'history-status-icon blocked'
                      }
                    >
                      ♢
                    </div>

                    <div className="history-main">
                      <div className="history-domain-row">
                        <h3>{item.domain}</h3>

                        <span
                          className={
                            item.status === 'safe'
                              ? 'history-badge safe'
                              : 'history-badge blocked'
                          }
                        >
                          {item.status === 'safe' ? 'SAFE' : 'BLOCKED'}
                        </span>
                      </div>

                      <p className="history-url">{item.url}</p>

                      <div className="history-divider"></div>

                      <div className="history-bottom-row">
                        <p>◷ {item.time}</p>

                        <div className="history-actions">
                          <button type="button">▣</button>
                          <button type="button">♜</button>
                        </div>
                      </div>
                    </div>
                  </div>
                ))}

                {filteredHistory.length === 0 && (
                  <p className="history-empty">No scan records found.</p>
                )}
              </div>
            </div>

            <BottomNav active="history" />
          </div>
        )}

        {screen === 'profile' && (
          <div className="profile-screen">
            <div className="profile-content">
              <h2 className="profile-title">Profile</h2>

              <div className="profile-user-row">
                <div className="profile-avatar">A</div>

                <div>
                  <h3>Alex Johnson</h3>
                  <p>alex@example.com</p>
                  <span>PRO PLAN</span>
                </div>
              </div>

              <p className="profile-section-label">Account</p>

              <div className="profile-menu-card">
                <button className="profile-menu-row" type="button">
                  <div className="profile-menu-icon">♙</div>

                  <div>
                    <h4>Account Information</h4>
                    <p>alex@example.com</p>
                  </div>

                  <span className="profile-chevron">›</span>
                </button>

                <button className="profile-menu-row" type="button">
                  <div className="profile-menu-icon">▢</div>

                  <div>
                    <h4>Change Password</h4>
                    <p>Last changed 30 days ago</p>
                  </div>

                  <span className="profile-chevron">›</span>
                </button>
              </div>

              <p className="profile-section-label">Security</p>

              <div className="profile-menu-card">
                <button className="profile-menu-row" type="button">
                  <div className="profile-menu-icon">♢</div>

                  <div>
                    <h4>Security Settings</h4>
                    <p>Two-factor authentication: ON</p>
                  </div>

                  <span className="profile-chevron">›</span>
                </button>

                <button className="profile-menu-row" type="button">
                  <div className="profile-menu-icon">♧</div>

                  <div>
                    <h4>Notification Preferences</h4>
                    <p>Threat alerts enabled</p>
                  </div>

                  <span className="profile-chevron">›</span>
                </button>
              </div>

              <p className="profile-section-label">Privacy & Legal</p>

              <div className="profile-menu-card">
                <button className="profile-menu-row" type="button">
                  <div className="profile-menu-icon">▢</div>

                  <div>
                    <h4>Privacy Policy</h4>
                    <p>Last updated Jun 2026</p>
                  </div>

                  <span className="profile-chevron">›</span>
                </button>

                <button className="profile-menu-row" type="button">
                  <div className="profile-menu-icon">ⓘ</div>

                  <div>
                    <h4>Terms of Service</h4>
                  </div>

                  <span className="profile-chevron">›</span>
                </button>
              </div>

              <button
                className="profile-signout-button"
                type="button"
                onClick={handleSignOut}
              >
                ↪ Sign Out
              </button>
            </div>

            <BottomNav active="profile" />
          </div>
        )}
      </section>
    </main>
  )
}

export default App